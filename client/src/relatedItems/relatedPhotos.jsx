import React from 'react';

function RelatedPhotos(props) {

if(props.relatedStylesInfo.results !== undefined && props.relatedStylesInfo.results[0].photos[0].thumbnail_url !== null)
  return(

  )

}

export default RelatedPhotos